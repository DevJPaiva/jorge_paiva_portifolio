{
  "name": "jorge-paiva-portifolio",
  "version": "1.0.0",
  "description": "Portfólio de UX/UI Designer Jorge Paiva",
  "main": "js/main.jsx",
  "scripts": {
    "dev": "esbuild ./js/main.jsx --bundle --outfile=./dist/bundle.js --loader:.jsx=jsx --watch --servedir=.",
    "build": "esbuild ./js/main.jsx --bundle --outfile=./dist/bundle.js --loader:.jsx=jsx --minify"
  },
  "keywords": [],
  "author": "Jorge Paiva",
  "license": "MIT",
  "devDependencies": {
    "esbuild": "^0.19.0"
  },
  "dependencies": {
    "react": "^18.0.0",
    "react-dom": "^18.0.0"
  }
}
